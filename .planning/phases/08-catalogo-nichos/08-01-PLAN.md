---
phase: 08-catalogo-nichos
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/backend/app.py
  - scripts/import/populate_niches.sql
  - tests/test_niches.py
autonomous: true
requirements: [NICHE-01, NICHE-03, NICHE-04]

must_haves:
  truths:
    - "GET /api/admin/niches returns all niches grouped by category with active/priority fields"
    - "GET /api/niches?active=true returns only active niches grouped by the 10 categories"
    - "PUT /api/admin/niches/<id> toggles active and updates priority"
    - "PUT /api/admin/niches/bulk enables or disables a batch of niches by IDs"
    - "populate_niches.sql inserts 150+ niches across 10 categories into the DB when run"
    - "Unauthenticated requests to /api/admin/niches return 401"
    - "tests/test_niches.py stubs exist and all pass (skip or xfail) without errors"
  artifacts:
    - path: "app/backend/app.py"
      provides: "niches table DDL in init_db() + 4 new endpoints"
      contains: "CREATE TABLE IF NOT EXISTS niches"
    - path: "scripts/import/populate_niches.sql"
      provides: "150+ niches SQL INSERT script"
      contains: "ON CONFLICT (name) DO NOTHING"
    - path: "tests/test_niches.py"
      provides: "Test stubs for all NICHE-01/02/03/04 behaviors"
      contains: "test_admin_get_niches_returns_catalog"
  key_links:
    - from: "init_db() in app.py"
      to: "niches table"
      via: "CREATE TABLE IF NOT EXISTS niches + INSERT from custom_niches migration"
      pattern: "CREATE TABLE IF NOT EXISTS niches"
    - from: "GET /api/admin/niches"
      to: "niches table"
      via: "SELECT id, name, category, active, priority, last_used_at FROM niches"
      pattern: "SELECT.*FROM niches"
    - from: "GET /api/niches"
      to: "niches table"
      via: "SELECT id, name, category, priority FROM niches WHERE active=true"
      pattern: "api/niches.*active"
---

<objective>
Create the `niches` database table, migrate existing `custom_niches` data, populate 150+ entries via SQL script, add 4 new endpoints (GET/PUT admin + GET public), and write the test scaffold.

Purpose: This is the foundation wave — nothing in Plans 02 and 03 can run without the `niches` table and its endpoints existing. The pipeline rotation logic (Plan 02) and the frontend admin page (Plan 03) both depend on data created here.

Output:
- `niches` table in PostgreSQL with 150+ rows populated
- `GET /api/admin/niches` — admin-only full catalog with all fields
- `PUT /api/admin/niches/<id>` — toggle individual niche active/priority
- `PUT /api/admin/niches/bulk` — bulk activate/deactivate by ID list
- `GET /api/niches?active=true` — public (auth required) grouped endpoint for frontend
- `tests/test_niches.py` — stubs for all 8 Phase 8 test cases
- `scripts/import/populate_niches.sql` — 150+ niches INSERT script
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-catalogo-nichos/08-CONTEXT.md
@.planning/phases/08-catalogo-nichos/08-RESEARCH.md

<interfaces>
<!-- Key existing code the executor must know. Read these before touching anything. -->

From app/backend/app.py line 870-906 — current pipeline config (will be modified in Plan 02, NOT this plan):
```python
DAILY_JOB_NICHES = [
    'restaurante', 'academia', 'clinica medica', 'dentista', 'advocacia',
    'contabilidade', 'imobiliaria', 'salao de beleza', 'farmacia',
    'supermercado', 'pizzaria', 'auto pecas', 'mecanica', 'escola',
    'hotel', 'pousada', 'sorveteria', 'padaria', 'pet shop',
]

def get_pipeline_config():
    """Read current pipeline config from DB. Falls back to module constants on any error."""
    try:
        with get_db() as conn:
            cur = conn.cursor()
            cur.execute("SELECT key, value FROM pipeline_config")
            rows = {k: json.loads(v) for k, v in cur.fetchall()}
        return {
            'niches':          rows.get('daily_niches',    DAILY_JOB_NICHES),
            'region':          rows.get('daily_region',    DAILY_JOB_REGION),
            'hour':            int(rows.get('daily_hour',  DAILY_JOB_HOUR)),
            'minute':          int(rows.get('daily_minute', 0)),
            'notify_email':    rows.get('notify_email'),
            'healthcheck_url': rows.get('healthcheck_url'),
        }
    except Exception as e:
        print(f"[CONFIG] Erro ao ler pipeline_config: {e} — usando defaults")
        return { 'niches': DAILY_JOB_NICHES, 'region': DAILY_JOB_REGION, ... }
```

From app/backend/app.py lines 2179-2185 — existing custom_niches table (data to be migrated):
```python
c.execute('''CREATE TABLE IF NOT EXISTS custom_niches (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(name)
)''')
```

From app/backend/app.py line 2172 — safe ALTER TABLE pattern:
```python
c.execute("ALTER TABLE system_logs ADD COLUMN IF NOT EXISTS fix_prompt TEXT")
```

From app/backend/app.py — admin auth check pattern used throughout:
```python
token = get_auth_header()
user_id = verify_token(token)
if not user_id:
    return jsonify({'error': 'Unauthorized'}), 401
with get_db() as conn:
    c = conn.cursor()
    c.execute('SELECT is_admin FROM users WHERE id=%s', (user_id,))
    row = c.fetchone()
    if not row or not row[0]:
        return jsonify({'error': 'Admin only'}), 403
```

From app/backend/app.py lines 15207-15270 — existing /api/niches/custom endpoints (keep intact):
```python
@app.route('/api/niches/custom', methods=['GET'])   # must keep working
@app.route('/api/niches/custom', methods=['POST'])  # must keep working
@app.route('/api/niches/custom/<path:name>', methods=['DELETE'])  # must keep working
```

From tests/conftest.py — test fixtures available:
```python
@pytest.fixture(scope="session") def api_base() -> str  # "https://api.extratordedados.com.br"
@pytest.fixture(scope="session") def auth_headers(auth_token)  # {"Authorization": "Bearer <token>"}
@pytest.fixture(scope="session") def auth_token(api_base, credentials) -> str
```

From app/frontend/components/Sidebar.tsx line 191-196 — isActive() exact-match list (context for Plan 03):
```typescript
if (href === '/admin' || href === '/dashboard' || href === '/leads' || ...) {
    return router.pathname === href
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: niches table DDL + custom_niches migration in init_db()</name>
  <read_first>
    - app/backend/app.py lines 2160-2210 (existing init_db migrations section — insert after custom_niches block at line 2185)
    - app/backend/app.py lines 870-906 (DAILY_JOB_NICHES and get_pipeline_config — DO NOT modify in this task)
  </read_first>
  <files>app/backend/app.py</files>
  <behavior>
    - After app startup, `SELECT COUNT(*) FROM niches` returns a row (table exists)
    - After app startup, any rows in `custom_niches` are also present in `niches` with `category='Outros'`
    - Running init_db() twice does not fail (idempotent — CREATE TABLE IF NOT EXISTS, INSERT ON CONFLICT DO NOTHING)
  </behavior>
  <action>
In `init_db()` in `app/backend/app.py`, immediately AFTER the `custom_niches` CREATE TABLE block (after line 2185, before the `enrichment_source` ALTER), insert the following two SQL statements:

```python
        # Phase 8: Niche catalog table
        c.execute('''CREATE TABLE IF NOT EXISTS niches (
            id           SERIAL PRIMARY KEY,
            name         VARCHAR(255) NOT NULL,
            category     VARCHAR(100) NOT NULL DEFAULT 'Outros',
            keywords     TEXT[]       DEFAULT '{}',
            active       BOOLEAN      DEFAULT TRUE,
            priority     INTEGER      DEFAULT 100,
            last_used_at TIMESTAMP,
            created_at   TIMESTAMP    DEFAULT NOW(),
            UNIQUE(name)
        )''')
        c.execute('CREATE INDEX IF NOT EXISTS idx_niches_active ON niches(active)')
        c.execute('CREATE INDEX IF NOT EXISTS idx_niches_last_used ON niches(last_used_at ASC NULLS FIRST)')
        # Migrate existing custom_niches rows (category = 'Outros', priority = 100)
        c.execute('''
            INSERT INTO niches (name, category, created_at)
            SELECT name, \'Outros\', created_at FROM custom_niches
            ON CONFLICT (name) DO NOTHING
        ''')
```

Do NOT modify `get_pipeline_config()`, `DAILY_JOB_NICHES`, or any existing endpoint. The migration INSERT uses `ON CONFLICT (name) DO NOTHING` — safe for concurrent Gunicorn workers.
  </action>
  <verify>
    <automated>pytest tests/test_niches.py -x -v -k "table" 2>/dev/null || echo "stubs not yet created — verify after Task 3"</automated>
  </verify>
  <done>
    - `app/backend/app.py` contains `CREATE TABLE IF NOT EXISTS niches` with all 8 columns (id, name, category, keywords, active, priority, last_used_at, created_at)
    - The migration INSERT selecting from `custom_niches` is present with `ON CONFLICT (name) DO NOTHING`
    - `grep -n "CREATE TABLE IF NOT EXISTS niches" app/backend/app.py` returns a result
  </done>
</task>

<task type="auto">
  <name>Task 2: populate_niches.sql — 150+ niches across 10 categories</name>
  <read_first>
    - .planning/phases/08-catalogo-nichos/08-CONTEXT.md (D-01: 10 category names in Portuguese)
  </read_first>
  <files>scripts/import/populate_niches.sql</files>
  <action>
Create `scripts/import/populate_niches.sql` with exactly this structure. Run on VPS: `psql -U extrator -d extrator -f populate_niches.sql`

The file must contain at least 150 INSERT rows using `INSERT INTO niches (name, category, priority, keywords) VALUES ... ON CONFLICT (name) DO NOTHING;`

Use these exact 10 category names (from D-01): `'Saúde'`, `'Beleza & Estética'`, `'Alimentação'`, `'Serviços Profissionais'`, `'Educação'`, `'Automotivo'`, `'Varejo & Comércio'`, `'Construção & Imóveis'`, `'Tecnologia'`, `'Turismo & Lazer'`

Minimum niches per category (must total 150+):

**Saúde (18 niches, priority 10):**
Clínica Médica, Clínica Odontológica, Clínica Veterinária, Clínica de Estética, Clínica de Fisioterapia, Clínica de Psicologia, Farmácia, Hospital Particular, Laboratório de Análises, Clínica de Nutrição, Clínica de Dermatologia, Clínica de Ortopedia, Clínica de Cardiologia, Clínica de Pediatria, Clínica de Oftalmologia, Clínica de Ginecologia, Clínica de Urologia, Centro Médico

**Beleza & Estética (15 niches, priority 20):**
Salão de Beleza, Barbearia, Estúdio de Tatuagem, Clínica de Depilação, Estúdio de Manicure, Spa, Centro de Estética, Loja de Cosméticos, Clínica de Micropigmentação, Studio de Pilates, Estúdio de Yoga, Centro de Massagem, Clínica de Podologia, Estúdio de Alongamento de Cílios, Instituto de Beleza

**Alimentação (18 niches, priority 15):**
Restaurante, Pizzaria, Hamburgueria, Padaria, Sorveteria, Cafeteria, Churrascaria, Lanchonete, Sushi, Doceria, Confeitaria, Empório, Bar e Petiscaria, Açougue, Marmitaria, Bistrô, Comida Árabe, Comida Japonesa

**Serviços Profissionais (18 niches, priority 20):**
Escritório de Advocacia, Escritório de Contabilidade, Consultoria Empresarial, Agência de Marketing, Agência de Publicidade, Seguradora, Corretora de Seguros, Despachante, Cartório, Tradutora, Gráfica, Empresa de Segurança, Agência de Recursos Humanos, Escritório de Arquitetura, Escritório de Engenharia, Consultoria de TI, Empresa de Logística, Empresa de Transporte

**Educação (15 niches, priority 20):**
Escola Particular, Escola de Idiomas, Escola de Música, Escola de Dança, Escola de Artes, Faculdade Particular, Curso Profissionalizante, Pré-Escola, Jardim de Infância, Escola de Futebol, Escola de Natação, Escola de Artes Marciais, Escola de Informática, Cursinho Pré-Vestibular, Escola de Gastronomia

**Automotivo (15 niches, priority 30):**
Oficina Mecânica, Auto Peças, Loja de Pneus, Lavagem de Carros, Auto Elétrica, Funilaria e Pintura, Concessionária, Despachante Veicular, Som Automotivo, Blindagem Veicular, Locadora de Carros, Posto de Gasolina, Borracharia, Distribuidor de Autopeças, Instaladora de Acessórios

**Varejo & Comércio (18 niches, priority 25):**
Supermercado, Farmácia Drogaria, Pet Shop, Loja de Roupas, Loja de Calçados, Loja de Móveis, Loja de Eletrodomésticos, Loja de Informática, Loja de Brinquedos, Loja de Artigos Esportivos, Ótica, Joalheria, Floricultura, Livraria, Papelaria, Loja de Presentes, Mercadinho, Bazar

**Construção & Imóveis (15 niches, priority 30):**
Imobiliária, Construtora, Empreiteira, Loja de Materiais de Construção, Empresa de Reforma, Empresa de Pintura Residencial, Empresa de Limpeza, Empresa de Dedetização, Empresa de Mudança, Marmoraria, Serralheria, Vidraçaria, Empresa de Energia Solar, Engenharia Civil, Empresa de Instalação Elétrica

**Tecnologia (12 niches, priority 40):**
Empresa de Software, Agência de Desenvolvimento Web, Empresa de Suporte de TI, Loja de Informática, Empresa de Cloud Computing, Startup de Tecnologia, Empresa de Cibersegurança, Empresa de Automação Industrial, Empresa de E-commerce, Empresa de Impressão 3D, Empresa de Inteligência Artificial, Empresa de App Mobile

**Turismo & Lazer (12 niches, priority 35):**
Hotel, Pousada, Hostel, Agência de Viagens, Parque de Diversões, Boliche, Clube Esportivo, Academia de Ginástica, Estúdio de Crossfit, Espaço para Eventos, Buffet, Casa de Festas

For each row, set keywords as `ARRAY['<lowercase variation 1>', '<lowercase variation 2>']` with 2-3 keywords relevant to the niche. Example: `ARRAY['clinica medica', 'consultorio medico', 'medico especialista']`

File header comment:
```sql
-- populate_niches.sql
-- Phase 8: Catálogo de Nichos — 150+ nichos iniciais distribuídos por 10 categorias
-- Run once on VPS: psql -U extrator -d extrator -f populate_niches.sql
-- Idempotent: ON CONFLICT (name) DO NOTHING
```
  </action>
  <verify>
    <automated>python -c "content=open('scripts/import/populate_niches.sql').read(); lines=[l for l in content.splitlines() if l.strip().startswith(\"('\")]; print(f'{len(lines)} niche rows found'); assert len(lines) >= 150, f'Need 150+ rows, found {len(lines)}'"</automated>
  </verify>
  <done>
    - `scripts/import/populate_niches.sql` exists and contains 150+ INSERT value rows
    - File contains all 10 category names exactly as specified in D-01
    - `grep -c "ON CONFLICT" scripts/import/populate_niches.sql` returns at least 1 (file is idempotent)
    - Each row includes a keywords ARRAY with at least 2 entries
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backend endpoints — GET/PUT /api/admin/niches + GET /api/niches + test stubs</name>
  <read_first>
    - app/backend/app.py lines 15207-15270 (existing /api/niches/custom endpoints — find correct insertion point AFTER these)
    - app/backend/app.py lines 15273-15285 (admin_logs endpoint after custom niches — insert before it)
    - tests/conftest.py (fixture signatures: api_base, auth_headers, auth_token)
    - .planning/phases/08-catalogo-nichos/08-VALIDATION.md (exact test function names required)
  </read_first>
  <files>app/backend/app.py, tests/test_niches.py</files>
  <behavior>
    - GET /api/admin/niches with valid admin token returns 200 with {"niches": {"Saúde": [...], ...}, "total": N}
    - GET /api/admin/niches without token returns 401
    - GET /api/admin/niches with non-admin token returns 403
    - PUT /api/admin/niches/<id> with {"active": false} sets that niche inactive, returns 200
    - PUT /api/admin/niches/<id> with {"priority": 5} sets priority, returns 200
    - PUT /api/admin/niches/bulk with {"ids": [1,2,3], "active": false} deactivates all three, returns 200
    - GET /api/niches?active=true with auth returns {"niches": {"Saúde": [...], ...}} with only active niches
    - GET /api/niches?active=true without token returns 401
  </behavior>
  <action>
In `app/backend/app.py`, add the following 4 endpoints AFTER the `/api/niches/custom/<path:name>` DELETE endpoint (after line 15270) and BEFORE the `@app.route('/api/admin/logs')` route:

```python
@app.route('/api/admin/niches', methods=['GET'])
@limiter.limit("60/minute")
def admin_get_niches():
    """Return all niches grouped by category. Admin only."""
    token = get_auth_header()
    user_id = verify_token(token)
    if not user_id:
        return jsonify({'error': 'Unauthorized'}), 401
    with get_db() as conn:
        c = conn.cursor()
        c.execute('SELECT is_admin FROM users WHERE id=%s', (user_id,))
        row = c.fetchone()
        if not row or not row[0]:
            return jsonify({'error': 'Admin only'}), 403
        c.execute('''
            SELECT id, name, category, active, priority, last_used_at, created_at
            FROM niches
            ORDER BY category, priority ASC, name ASC
        ''')
        rows = c.fetchall()
    from collections import defaultdict
    grouped = defaultdict(list)
    for r in rows:
        grouped[r[2]].append({
            'id': r[0], 'name': r[1], 'category': r[2],
            'active': r[3], 'priority': r[4],
            'last_used_at': r[5].isoformat() if r[5] else None,
            'created_at': r[6].isoformat() if r[6] else None,
        })
    return jsonify({'niches': dict(grouped), 'total': len(rows)}), 200


@app.route('/api/admin/niches/<int:niche_id>', methods=['PUT'])
@limiter.limit("120/minute")
def admin_update_niche(niche_id):
    """Toggle active or update priority for a single niche. Admin only."""
    token = get_auth_header()
    user_id = verify_token(token)
    if not user_id:
        return jsonify({'error': 'Unauthorized'}), 401
    with get_db() as conn:
        c = conn.cursor()
        c.execute('SELECT is_admin FROM users WHERE id=%s', (user_id,))
        row = c.fetchone()
        if not row or not row[0]:
            return jsonify({'error': 'Admin only'}), 403
        data = request.get_json() or {}
        updates = []
        params = []
        if 'active' in data:
            updates.append('active = %s')
            params.append(bool(data['active']))
        if 'priority' in data:
            updates.append('priority = %s')
            params.append(int(data['priority']))
        if not updates:
            return jsonify({'error': 'Nenhum campo para atualizar'}), 400
        params.append(niche_id)
        try:
            c.execute(f'UPDATE niches SET {", ".join(updates)} WHERE id = %s RETURNING id, name, active, priority', params)
            updated = c.fetchone()
            conn.commit()
        except Exception as e:
            conn.rollback()
            return jsonify({'error': str(e)}), 500
    if not updated:
        return jsonify({'error': 'Nicho não encontrado'}), 404
    return jsonify({'id': updated[0], 'name': updated[1], 'active': updated[2], 'priority': updated[3]}), 200


@app.route('/api/admin/niches/bulk', methods=['PUT'])
@limiter.limit("30/minute")
def admin_bulk_toggle_niches():
    """Activate or deactivate multiple niches by ID list. Admin only."""
    token = get_auth_header()
    user_id = verify_token(token)
    if not user_id:
        return jsonify({'error': 'Unauthorized'}), 401
    with get_db() as conn:
        c = conn.cursor()
        c.execute('SELECT is_admin FROM users WHERE id=%s', (user_id,))
        row = c.fetchone()
        if not row or not row[0]:
            return jsonify({'error': 'Admin only'}), 403
        data = request.get_json() or {}
        ids = data.get('ids', [])
        active = data.get('active', True)
        if not ids or not isinstance(ids, list):
            return jsonify({'error': 'ids deve ser lista não vazia'}), 400
        try:
            c.execute('UPDATE niches SET active = %s WHERE id = ANY(%s)', (bool(active), ids))
            conn.commit()
        except Exception as e:
            conn.rollback()
            return jsonify({'error': str(e)}), 500
    return jsonify({'updated': len(ids), 'active': bool(active)}), 200


@app.route('/api/niches', methods=['GET'])
@limiter.limit("120/minute")
def get_niches_catalog():
    """Return niches grouped by category. Auth required. ?active=true filters to active only."""
    token = get_auth_header()
    user_id = verify_token(token)
    if not user_id:
        return jsonify({'error': 'Unauthorized'}), 401
    active_only = request.args.get('active', '').lower() == 'true'
    with get_db() as conn:
        c = conn.cursor()
        if active_only:
            c.execute('SELECT id, name, category, priority FROM niches WHERE active = TRUE ORDER BY category, priority ASC, name ASC')
        else:
            c.execute('SELECT id, name, category, priority, active FROM niches ORDER BY category, priority ASC, name ASC')
        rows = c.fetchall()
    from collections import defaultdict
    grouped = defaultdict(list)
    for r in rows:
        entry = {'id': r[0], 'name': r[1], 'priority': r[3]}
        if not active_only:
            entry['active'] = r[4]
        grouped[r[2]].append(entry)
    return jsonify({'niches': dict(grouped)}), 200
```

IMPORTANT: The route `/api/admin/niches/bulk` must be registered BEFORE `/api/admin/niches/<int:niche_id>` in the file, otherwise Flask will try to match "bulk" as an integer ID and return 404. Check Flask route order — register bulk PUT first.

---

Then create `tests/test_niches.py` with these exact function names (stubs using `pytest.mark.xfail` so suite is green before implementation):

```python
"""
Smoke tests for Phase 8: Catálogo de Nichos.
Run: pytest tests/test_niches.py -x -v
Full suite: pytest tests/ -x
"""
import pytest
import requests

pytestmark = pytest.mark.usefixtures()


def test_admin_niches_requires_auth(api_base):
    """GET /api/admin/niches without token returns 401."""
    resp = requests.get(f"{api_base}/api/admin/niches", timeout=10)
    assert resp.status_code == 401


def test_admin_get_niches_returns_catalog(api_base, auth_headers):
    """GET /api/admin/niches returns catalog grouped by category with total."""
    resp = requests.get(f"{api_base}/api/admin/niches", headers=auth_headers, timeout=10)
    assert resp.status_code == 200
    data = resp.json()
    assert 'niches' in data
    assert 'total' in data
    assert isinstance(data['niches'], dict)
    assert len(data['niches']) >= 1


def test_niches_catalog_count_gte_150(api_base, auth_headers):
    """GET /api/admin/niches returns at least 150 niches total after populate_niches.sql."""
    resp = requests.get(f"{api_base}/api/admin/niches", headers=auth_headers, timeout=10)
    assert resp.status_code == 200
    data = resp.json()
    assert data.get('total', 0) >= 150, f"Expected 150+ niches, got {data.get('total')}"


def test_admin_toggle_niche_active(api_base, auth_headers):
    """PUT /api/admin/niches/<id> toggles active field."""
    # Get first niche ID
    resp = requests.get(f"{api_base}/api/admin/niches", headers=auth_headers, timeout=10)
    assert resp.status_code == 200
    niches = resp.json().get('niches', {})
    all_niches = [n for cat in niches.values() for n in cat]
    if not all_niches:
        pytest.skip("No niches in catalog yet — run populate_niches.sql first")
    niche_id = all_niches[0]['id']
    current_active = all_niches[0]['active']

    # Toggle
    toggle_resp = requests.put(
        f"{api_base}/api/admin/niches/{niche_id}",
        json={'active': not current_active},
        headers=auth_headers,
        timeout=10
    )
    assert toggle_resp.status_code == 200
    assert toggle_resp.json()['active'] == (not current_active)

    # Restore
    requests.put(
        f"{api_base}/api/admin/niches/{niche_id}",
        json={'active': current_active},
        headers=auth_headers,
        timeout=10
    )


def test_admin_bulk_toggle_niches(api_base, auth_headers):
    """PUT /api/admin/niches/bulk deactivates a batch of niches."""
    resp = requests.get(f"{api_base}/api/admin/niches", headers=auth_headers, timeout=10)
    assert resp.status_code == 200
    niches = resp.json().get('niches', {})
    all_niches = [n for cat in niches.values() for n in cat]
    if len(all_niches) < 3:
        pytest.skip("Not enough niches to test bulk — run populate_niches.sql first")
    ids = [n['id'] for n in all_niches[:3]]

    bulk_resp = requests.put(
        f"{api_base}/api/admin/niches/bulk",
        json={'ids': ids, 'active': False},
        headers=auth_headers,
        timeout=10
    )
    assert bulk_resp.status_code == 200
    assert bulk_resp.json()['updated'] == 3
    assert bulk_resp.json()['active'] == False

    # Restore
    requests.put(
        f"{api_base}/api/admin/niches/bulk",
        json={'ids': ids, 'active': True},
        headers=auth_headers,
        timeout=10
    )


def test_public_niches_endpoint_grouped(api_base, auth_headers):
    """GET /api/niches?active=true returns niches grouped by category."""
    resp = requests.get(f"{api_base}/api/niches?active=true", headers=auth_headers, timeout=10)
    assert resp.status_code == 200
    data = resp.json()
    assert 'niches' in data
    assert isinstance(data['niches'], dict)


def test_public_niches_has_10_categories(api_base, auth_headers):
    """GET /api/niches?active=true returns niches in exactly 10 categories."""
    resp = requests.get(f"{api_base}/api/niches?active=true", headers=auth_headers, timeout=10)
    assert resp.status_code == 200
    data = resp.json()
    categories = list(data['niches'].keys())
    assert len(categories) >= 10, f"Expected 10 categories, got {len(categories)}: {categories}"


def test_pipeline_config_niches_from_db(api_base, auth_headers):
    """GET /api/admin/pipeline-config returns niches key sourced from DB (not hardcoded list).
    This test is activated in Plan 02 after get_pipeline_config() is modified.
    For now, verifies that the endpoint still works (backward compat)."""
    resp = requests.get(f"{api_base}/api/admin/pipeline-config", headers=auth_headers, timeout=10)
    assert resp.status_code == 200
    data = resp.json()
    assert 'niches' in data
    assert isinstance(data['niches'], list)
```
  </action>
  <verify>
    <automated>pytest tests/test_niches.py -x -v --tb=short</automated>
  </verify>
  <done>
    - `app/backend/app.py` contains all 4 new routes: `GET /api/admin/niches`, `PUT /api/admin/niches/<int:niche_id>`, `PUT /api/admin/niches/bulk`, `GET /api/niches`
    - Route `/api/admin/niches/bulk` is defined BEFORE `/api/admin/niches/<int:niche_id>` in the file
    - `tests/test_niches.py` exists with all 8 test functions
    - `pytest tests/test_niches.py -x -v` passes (tests that require populated DB will skip gracefully)
    - `grep -c "def test_" tests/test_niches.py` returns 8
  </done>
</task>

<task type="auto">
  <name>Task 4: Deploy, smoke test, and populate niches on VPS</name>
  <read_first>
    - deploy.py (root — confirm VPS deploy command pattern)
    - .deploy.env (confirm credentials present, do not log values)
  </read_first>
  <files>app/backend/app.py</files>
  <action>
1. **Self-review**: Read the diff of changes made to `app/backend/app.py` in this plan. Verify:
   - `CREATE TABLE IF NOT EXISTS niches` is inside `init_db()`, not at module level
   - Route `/api/admin/niches/bulk` is defined BEFORE `/api/admin/niches/<int:niche_id>`
   - No existing routes were deleted or modified
   - `get_pipeline_config()` was NOT touched (that is Plan 02's work)
   - All 4 new endpoints use the admin auth check pattern consistently

2. **Deploy backend**: `python deploy.py backend`

3. **Health check**: `curl -s https://api.extratordedados.com.br/api/health` — expect `{"status":"ok",...}`

4. **Populate niches on VPS**: SSH to VPS and run the SQL script:
   ```bash
   ssh root@185.173.110.180 "cd /app && docker exec extrator-postgres psql -U extrator -d extrator -f /dev/stdin" < scripts/import/populate_niches.sql
   ```
   Alternatively: Copy the file to VPS then run:
   ```bash
   scp scripts/import/populate_niches.sql root@185.173.110.180:/tmp/
   ssh root@185.173.110.180 "docker exec -i extrator-postgres psql -U extrator -d extrator < /tmp/populate_niches.sql"
   ```

5. **Regression test**: `pytest tests/ -x --tb=short`

6. **Commit**:
   ```bash
   git add app/backend/app.py scripts/import/populate_niches.sql tests/test_niches.py
   git commit -m "feat(08-01): niches table + populate script + admin CRUD endpoints + test stubs"
   ```
  </action>
  <verify>
    <automated>pytest tests/ -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s https://api.extratordedados.com.br/api/health | python -c "import sys,json;d=json.load(sys.stdin);assert d['status']=='ok'"` exits 0
    - `pytest tests/test_niches.py -x -v` — all 8 tests pass or skip (none fail)
    - `pytest tests/ -x --tb=short` — full suite green
    - Git log shows new commit with message containing "08-01"
  </acceptance_criteria>
  <done>
    - Backend deployed and health check passes
    - Niches table populated with 150+ rows on VPS
    - Full test suite green
    - Changes committed
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `curl -s -H "Authorization: Bearer $TOKEN" https://api.extratordedados.com.br/api/admin/niches | python -c "import sys,json;d=json.load(sys.stdin);print('Total:', d['total']); assert d['total'] >= 150"` — must print Total: 150+
2. `curl -s -H "Authorization: Bearer $TOKEN" "https://api.extratordedados.com.br/api/niches?active=true" | python -c "import sys,json;d=json.load(sys.stdin);cats=list(d['niches'].keys());print('Categories:', len(cats), cats); assert len(cats) >= 10"` — must print 10 category names
3. `curl -s https://api.extratordedados.com.br/api/admin/niches` returns `{"error":"Unauthorized"}` with status 401
4. `pytest tests/test_niches.py -x -v` — all 8 tests pass or skip
5. `pytest tests/ -x` — full suite green
</verification>

<success_criteria>
- niches table exists in PostgreSQL with correct schema
- 150+ niches inserted across 10 categories (D-01)
- populate_niches.sql is idempotent (safe to run twice)
- custom_niches rows migrated to niches table (ON CONFLICT DO NOTHING)
- GET /api/admin/niches returns all niches grouped by category
- PUT /api/admin/niches/<id> updates active/priority for a single niche
- PUT /api/admin/niches/bulk updates active for a list of niches
- GET /api/niches?active=true returns only active niches grouped by category
- All existing /api/niches/custom endpoints still work (backward compat)
- test_niches.py created with all 8 test stubs
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-catalogo-nichos/08-01-SUMMARY.md`
</output>
